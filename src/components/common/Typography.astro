---
interface Props {
  className?: string;
  tag?: keyof HTMLElementTagNameMap;
}

const { className, tag = 'p' } = Astro.props;
const Tag = tag;
---

<Tag class:list={['text-base lg:text-xl text-black', className]}>
  <slot />
</Tag>